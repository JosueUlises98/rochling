package org.kopingenieria.model.enums.network;

public enum ConnectionStatus {
    DISCONNECTED,
    CONNECTING,
    CONNECTED,
    FAILED,
    ERROR,
    RECONNECTING,
    DISCONNECTING,
    UNKNOWN
}
