package org.kopingenieria.model.enums.network.connection;

public enum ConnectionStatus {
    DISCONNECTED,
    CONNECTING,
    CONNECTED,
    FAILED,
    ERROR,
    RECONNECTING,
    DISCONNECTING,
    UNKNOWN
}
